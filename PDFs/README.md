# PDF uploads

Add `.pdf` files to this folder. GitHub Pages automatically copies them to the site, and the Archive lists each file with Preview and Download actions.

The filename becomes the visible document title. Use hyphens or underscores between words if needed.
